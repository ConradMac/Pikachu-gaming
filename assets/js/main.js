// import { pokemonData, trainerData, enemyData, locationData } from "./data.js";

//! 1. Create a function that takes in an array of objects and returns a new array with only the names of the objects.
